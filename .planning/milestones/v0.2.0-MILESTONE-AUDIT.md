---
milestone: v0.2.0
audited: 2026-05-18T00:00:00Z
status: gaps_found
scores:
  requirements: 16/17
  phases: 5/5
  integration: 14/15
  flows: 4/4
gaps:
  requirements:
    - id: "PERF-01"
      status: "unsatisfied"
      phase: "Phase 5 (token-counting-timing)"
      claimed_by_plans: ["05-01-PLAN.md", "05-02-PLAN.md"]
      completed_by_plans: ["05-01-SUMMARY.md", "05-02-SUMMARY.md"]
      verification_status: "stale — passed at Phase 5 but invalidated by Phase 6 NarrationEvent deletion"
      evidence: "step_duration_ms is computed in runner.py:541 and written to JSONL (line 418) but never emitted as an SSE field. Phase 6 deleted NarrationEvent (commit 913c551) which carried this field. ActionDetailEvent has no duration field. No per-step timing appears in the narration feed. UI shows only wall-clock total elapsed time (index.html:114)."
  integration:
    - "PERF-01: step_duration_ms computed in _log_step but ActionDetailEvent has no duration field — UI narration feed has no per-step timing display"
  flows: []
tech_debt:
  - phase: 07-screenshot-streaming
    items:
      - "Human verification pending: live browser test for SC4 ('displayed screenshot never more than one step behind') — VERIFICATION.md status is human_needed (5/6 truths)"
  - phase: 08-training-data-enrichment
    items:
      - "memory field from ThoughtEvent/_pre_step is NOT stored in run_thoughts dict and NOT written to JSONL (runner.py lines 525–528 omit it). memory is emitted to SSE for UI only."
      - "training/converter.py, train_nvidia.py, train_apple.py have no built-in path resolution to get_user_data_dir() JSONL location — users must manually pass --input <path>"
      - "If process hard-crashes (SIGKILL/OOM) before run_agent finally block executes, records have no run_success field and are silently excluded by converter (expected behavior, no diagnostic)"
      - "TODO (v0.3.0) markers in train_nvidia.py:111 and train_apple.py:67 — version-anchored, acceptable"
  - phase: 09-frontend-polish
    items:
      - "extract and done action types reuse action-badge-scroll (gray) and action-badge-click (green) CSS classes — no distinct visual differentiation for terminal done state"
  nyquist:
    compliant_phases: []
    partial_phases: [05, 06, 07, 08, 09]
    missing_phases: []
    overall: PARTIAL
    notes: "All 5 phase VALIDATION.md files exist but have nyquist_compliant: false and wave_0_complete: false (status: draft)"
---

# v0.2.0 Foundations — Milestone Audit

**Audited:** 2026-05-18
**Status:** ⚠ gaps_found
**Phases:** 5-9 (5 phases, 10 plans)

---

## Summary

| Dimension | Score | Status |
|-----------|-------|--------|
| Requirements | 16/17 satisfied | ⚠ 1 gap |
| Phase verifications | 5/5 complete | ✅ (Phase 7 has 1 human-needed item) |
| Integration wiring | 14/15 connected | ⚠ 1 partial (PERF-01) |
| E2E flows | 4/4 complete | ✅ |
| Nyquist compliance | 0/5 phases | ⚠ all PARTIAL |

---

## Gap: PERF-01 (Unsatisfied)

**Requirement:** User sees per-step elapsed time next to each narration entry (e.g., "3.2s")

**Root cause:** Phase 5 satisfied PERF-01 by adding `step_duration_ms` to `NarrationEvent`. Phase 6 then deleted `NarrationEvent` (commit `913c551` — "delete NarrationEvent + handleNarration + _pendingAction (dead code)"). The `step_duration_ms` field was NOT migrated to `ActionDetailEvent` or any other SSE event type.

**Current state:**
- `step_duration_ms` is computed in `runner.py:541` (`duration_ms = int((time.monotonic() - step_start) * 1000)`)
- Written to JSONL record at `runner.py:418`
- NOT present on `ActionDetailEvent` dataclass (`agent/events.py:69-79`)
- NOT emitted in any SSE event reaching the UI
- UI shows only wall-clock total elapsed time (`index.html:114`: `elapsed.s`) — not per-step duration

**Evidence:**
```
grep step_duration_ms agent/events.py   → 0 hits
grep step_duration_ms agent/runner.py   → 1 hit (JSONL write only, line 418)
grep step_duration_ms agent/templates/  → 0 hits
```

**Phase history:**
- Phase 5 VERIFICATION.md: SATISFIED ("NarrationEvent carries a step_duration_ms int field") — valid at verification time
- Phase 6 deleted NarrationEvent — phase 5's verification became stale
- Phase 9 VERIFICATION.md: does not reference PERF-01 (only PERF-03, UI-01, UI-02)
- REQUIREMENTS.md traceability: PERF-01 still shows "Pending"

---

## Requirements Coverage (3-Source Cross-Reference)

| REQ-ID | VERIFICATION.md | SUMMARY frontmatter | REQUIREMENTS.md | Final Status |
|--------|-----------------|---------------------|-----------------|--------------|
| PERF-01 | Phase 5: SATISFIED (stale — path broken by P6 deletion) | Not present | [ ] Pending | **unsatisfied** |
| PERF-02 | Phase 5: SATISFIED | Not present | [ ] Pending | **satisfied** (checkbox stale) |
| PERF-04 | Phase 5: SATISFIED | Not present | [ ] Pending | **satisfied** (checkbox stale) |
| TRANS-01 | Phase 6: SATISFIED | Not present | [ ] Pending | **satisfied** (checkbox stale) |
| TRANS-02 | Phase 6: SATISFIED | Not present | [ ] Pending | **satisfied** (checkbox stale) |
| TRANS-03 | Phase 6: SATISFIED | Not present | [ ] Pending | **satisfied** (checkbox stale) |
| SCR-01 | Phase 7: SATISFIED | Not present | [ ] Pending | **satisfied** (checkbox stale) |
| SCR-02 | Phase 7: SATISFIED | Not present | [ ] Pending | **satisfied** (checkbox stale) |
| TRAIN-01 | Phase 8: SATISFIED | Not present | [x] Complete | **satisfied** |
| TRAIN-02 | Phase 8: SATISFIED | Not present | [x] Complete | **satisfied** |
| TRAIN-03 | Phase 8: SATISFIED | Not present | [x] Complete | **satisfied** |
| TRAIN-04 | Phase 8: SATISFIED | Not present | [x] Complete | **satisfied** |
| TRAIN-05 | Phase 8: SATISFIED | Not present | [x] Complete | **satisfied** |
| TRAIN-06 | Phase 8: SATISFIED | Not present | [x] Complete | **satisfied** |
| PERF-03 | Phase 9: SATISFIED | Not present | [ ] Pending | **satisfied** (checkbox stale) |
| UI-01 | Phase 9: SATISFIED | Not present | [ ] Pending | **satisfied** (checkbox stale) |
| UI-02 | Phase 9: SATISFIED | Not present | [ ] Pending | **satisfied** (checkbox stale) |

Note: REQUIREMENTS.md traceability checkboxes are stale for 11 requirements. Phase VERIFICATION.md files are authoritative; stale checkboxes are an update task for milestone close, not a gap.

Note: SUMMARY.md files do not use `requirements-completed` YAML frontmatter — this source is absent for all phases. The 3-source matrix routes to "partial (verify manually)" when SUMMARY is missing; all were manually verified via VERIFICATION.md files.

---

## Phase Verification Status

| Phase | Status | Score | Notes |
|-------|--------|-------|-------|
| 05 Token Counting + Timing | ✅ passed | 9/9 | All truths verified |
| 06 Model Transparency | ✅ passed | 4/4 | All truths verified |
| 07 Screenshot Streaming | ⚠ human_needed | 5/6 | SC4 (live browser timing) requires human confirmation |
| 08 Training Data Enrichment | ✅ passed | 6/6 | All truths verified |
| 09 Frontend Polish | ✅ passed | 5/5 | All truths verified |

---

## Integration Check Results

**E2E Flows (all complete):**

1. User task submission → /run → SSE stream → all 9 event types → UI handlers — **WIRED**
2. Run completes → DoneEvent → htmx:sseClose → GET /runs → runs_fragment.html — **WIRED**
3. Token/cost ticker: TokenEvent → handleToken → tickerText() → header-ticker span — **WIRED**
4. Training: log_step → JSONL → _rewrite_jsonl_run_success → converter — **WIRED**

**Integration Gaps:**

- **PERF-01 (gap):** `step_duration_ms` computed in `_log_step` → JSONL only; no SSE emission path, no narration row field. `ActionDetailEvent` has no timing field.

**Warnings (non-blocking):**

- `memory` field in `ThoughtEvent` and `_pre_step` is NOT stored in `run_thoughts` dict and NOT written to JSONL record. If `memory` is needed as a training signal, add it to `run_thoughts` and the JSONL schema.
- Training scripts (`converter.py`, `train_nvidia.py`, `train_apple.py`) have no built-in path resolution to `get_user_data_dir()` JSONL location.
- `extract` and `done` action types reuse `action-badge-scroll` (gray) and `action-badge-click` (green) — no distinct visual class for terminal state.
- If process hard-crashes before `finally` block, JSONL records for that run have no `run_success` field (excluded by converter — intentional but silent).

---

## Nyquist Coverage

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Action |
|-------|---------------|-------------------|-----------------|--------|
| 05 | exists | false | false | `/gsd-validate-phase 5` |
| 06 | exists | false | false | `/gsd-validate-phase 6` |
| 07 | exists | false | false | `/gsd-validate-phase 7` |
| 08 | exists | false | false | `/gsd-validate-phase 8` |
| 09 | exists | false | false | `/gsd-validate-phase 9` |

All VALIDATION.md files are in `status: draft` state. Nyquist wave-0 was not run for any v0.2.0 phase. This is a coverage gap — not a functionality blocker but the test suite has no wave-based sampling.

---

## Tech Debt Summary

**Phase 07 — Screenshot Streaming:**
- Human verification pending for SC4 (live browser + LLM timing test). VERIFICATION.md status is `human_needed`.

**Phase 08 — Training Data Enrichment:**
- `memory` field from `ThoughtEvent` is not persisted to JSONL (may be intentional — not required by TRAIN-01 spec)
- Training scripts require manual `--input <path>` pointing to platform-specific JSONL location
- `TODO (v0.3.0)` markers in training scripts (version-anchored, acceptable)

**Phase 09 — Frontend Polish:**
- `extract` and `done` action types share CSS classes with other badge types

**All phases:**
- Nyquist VALIDATION.md files are draft/incomplete

---

_Audit generated: 2026-05-18_
_Auditor: Claude (gsd-audit-milestone)_

---
phase: 09.1
plan: 02
subsystem: agent/ui/tests
tags: [perf-01, step-duration, sse, tdd-green, action-detail]
dependency_graph:
  requires: [09.1-01]
  provides: [agent/events.py:ActionDetailEvent.step_duration_ms, agent/runner.py:_log_step wiring, agent/templates/index.html:handleActionDetail timestamp span]
  affects: [tests/unit/test_events_phase5.py, tests/unit/test_events_phase6.py, tests/unit/test_events_phase9_1.py]
tech_stack:
  added: []
  patterns: [Optional[int] dataclass field appended last, idempotent DOM span guard (d.field != null && !row.querySelector), toFixed(1) formatting for elapsed time display]
key_files:
  created: []
  modified:
    - agent/events.py
    - agent/runner.py
    - agent/templates/index.html
    - tests/unit/test_events_phase5.py
    - tests/unit/test_events_phase6.py
decisions:
  - "Appended step_duration_ms as last field on ActionDetailEvent to preserve backward-compatibility with all existing keyword-arg callers"
  - "Used d.step_duration_ms != null (loose null check) in JS to catch both null and undefined from SSE payload"
  - "No new CSS class added — reused existing .timestamp class (font-size 11px, var(--text-placeholder), flex-shrink 0)"
  - "Pre-existing test_events_phase8.py::test_jsonl_enriched_fields_anthropic failure confirmed pre-existing (not introduced by this plan); deferred"
metrics:
  duration: ~10m
  completed: 2026-05-18
  tasks_completed: 2
  tasks_total: 3
---

# Phase 09.1 Plan 02: Wire step_duration_ms End-to-End (PERF-01) Summary

**One-liner:** step_duration_ms wired from ActionDetailEvent dataclass field through _log_step kwarg to handleActionDetail .timestamp span rendering "X.Xs" per narration row.

## What Was Built

### Task 1: ActionDetailEvent field + _log_step wiring

- Added `step_duration_ms: Optional[int] = None` as the last field on `ActionDetailEvent` in `agent/events.py`
- Appended `step_duration_ms=duration_ms` kwarg to the `ActionDetailEvent(...)` constructor call in `_log_step` in `agent/runner.py` — `duration_ms` was already in scope at line 541
- All 3 RED tests from Plan 01 (`test_events_phase9_1.py`) now pass GREEN:
  - `test_action_detail_event_has_step_duration_ms_field`
  - `test_action_detail_event_duration_roundtrip`
  - `test_log_step_puts_duration_on_action_detail_event`

### Task 2: handleActionDetail .timestamp span + test updates

- Added idempotent `.timestamp` span append to `handleActionDetail` in `agent/templates/index.html`:
  - Guard: `d.step_duration_ms != null && !row.querySelector('.timestamp')`
  - Content: `(d.step_duration_ms / 1000).toFixed(1) + 's'` (e.g., "3.2s")
  - Uses existing `.timestamp` CSS class — no new CSS added
- Fixed outdated docstring in `test_events_phase5.py::test_log_step_timing_closure_executes` (removed "no longer surfaced on any SSE event", replaced with correct statement about PERF-01 surfacing on ActionDetailEvent)
- Added `step_duration_ms` assertions to `test_events_phase6.py`:
  - `test_action_detail_event_shape`: `assert evt.step_duration_ms is None` (default block) + `step_duration_ms=250` kwarg + `assert evt2.step_duration_ms == 250` (keyword block)
  - `test_action_detail_event_null_fields`: `assert "step_duration_ms" in asdict_result` + `assert asdict_result["step_duration_ms"] is None`

## Task Commits

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add step_duration_ms to ActionDetailEvent and wire _log_step | 0fdf9ae | agent/events.py, agent/runner.py |
| 2 | Add .timestamp span in handleActionDetail; update phase5/6 tests | 5e8a049 | agent/templates/index.html, tests/unit/test_events_phase5.py, tests/unit/test_events_phase6.py |

## Verification Results

```
tests/unit/test_events_phase9_1.py ... 3 passed in 1.50s
191 passed in 16.63s (excluding pre-existing phase8 failure)
```

Acceptance criteria grep checks:
- `grep -c "d.step_duration_ms" agent/templates/index.html` → 2 (guard + toFixed line)
- `grep -nE "d\.step_duration_ms != null"` → 1 match (line 452)
- `grep -nE "row\.querySelector\('\.timestamp'\)"` → 1 match (line 452)
- `grep -nE "toFixed\(1\) \+ 's'"` → 1 match (line 455)
- `grep -n "no longer surfaced" test_events_phase5.py` → 0 matches
- `grep -n "surfaced on ActionDetailEvent" test_events_phase5.py` → 1 match
- `grep -c "step_duration_ms" test_events_phase6.py` → 5 matches
- `grep -nE "step_duration_ms=250"` → 1 match
- `grep -nE 'asdict_result\["step_duration_ms"\] is None'` → 1 match

## Deviations from Plan

### Pre-existing failure logged (not fixed)

**`test_events_phase8.py::test_jsonl_enriched_fields_anthropic`** was already failing before this plan (confirmed by git stash test — same failure on base commit `d2b7724`). The test asserts `model_thought` is a non-null string, but it returns `None` in the fake agent context. This is out of scope for this plan.

Logged to deferred items.

## Known Stubs

None. All wiring is end-to-end with real data.

## Threat Flags

None. No new network endpoints, auth paths, or security-relevant surface introduced.

## Self-Check: PASSED

- [x] `agent/events.py` has `step_duration_ms: Optional[int] = None` as last field in ActionDetailEvent
- [x] `agent/runner.py` has `step_duration_ms=duration_ms` in `_log_step` ActionDetailEvent call (exactly 1 match)
- [x] `agent/templates/index.html` has idempotent `.timestamp` span with `d.step_duration_ms != null` guard
- [x] `toFixed(1) + 's'` formatting present in index.html
- [x] `tests/unit/test_events_phase5.py` no longer contains "no longer surfaced"; contains "surfaced on ActionDetailEvent"
- [x] `tests/unit/test_events_phase6.py` has 5 `step_duration_ms` mentions
- [x] All 3 RED tests from Plan 01 now pass GREEN
- [x] 191 unit tests pass (pre-existing phase8 failure excluded)
- [x] Commits 0fdf9ae and 5e8a049 verified via `git log --oneline`

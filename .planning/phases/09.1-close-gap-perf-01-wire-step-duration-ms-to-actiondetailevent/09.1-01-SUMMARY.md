---
phase: 09.1
plan: 01
subsystem: tests
tags: [tdd, red-gate, perf-01, step-duration]
dependency_graph:
  requires: []
  provides: [tests/unit/test_events_phase9_1.py]
  affects: []
tech_stack:
  added: []
  patterns: [FakeAgentClass + asyncio.sleep + queue-drain integration test, dataclasses.asdict shape test]
key_files:
  created:
    - tests/unit/test_events_phase9_1.py
  modified: []
decisions:
  - "Used >= 0 for duration assertion to handle fast CI rounding to 0ms"
  - "Reused FakeAgentClass pattern from test_events_phase5.py with asyncio.sleep(0.010) for measurable timing"
  - "Module-level _make_fake_agent_with_tokens() with empty usage_history (Ollama path) per plan spec"
metrics:
  duration: ~1m
  completed: 2026-05-18
  tasks_completed: 1
  tasks_total: 1
---

# Phase 09.1 Plan 01: RED Test Suite for ActionDetailEvent.step_duration_ms (PERF-01) Summary

**One-liner:** RED test gate proving ActionDetailEvent lacks step_duration_ms field and _log_step does not pass duration_ms — 3 tests all fail with AttributeError.

## What Was Built

Created `tests/unit/test_events_phase9_1.py` with exactly 3 RED tests establishing the Nyquist gate for PERF-01:

1. **`test_action_detail_event_has_step_duration_ms_field`** (synchronous) — Constructs `ActionDetailEvent()` and asserts `step_duration_ms is None`; constructs with `step_duration_ms=1234` and checks value + `dataclasses.asdict` round-trip. Fails with `AttributeError: 'ActionDetailEvent' object has no attribute 'step_duration_ms'`.

2. **`test_action_detail_event_duration_roundtrip`** (synchronous) — Constructs `ActionDetailEvent(step=1, action_type="click_element", step_duration_ms=1234)`, calls `dataclasses.asdict`, asserts `result["step_duration_ms"] == 1234`. Fails with same AttributeError.

3. **`test_log_step_puts_duration_on_action_detail_event`** (async, uses `training_dir` + `monkeypatch_env` fixtures) — Integration test using `FakeAgentClass` with `asyncio.sleep(0.010)` before calling `on_step_end`. Drains queue, filters `ActionDetailEvent` instances, asserts `step_duration_ms is not None` and `>= 0`. Fails with `AttributeError` on the assertion even though the event is emitted.

## Task Commits

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create RED test suite for PERF-01 | 8a4b1ea | tests/unit/test_events_phase9_1.py |

## Verification Results

```
FAILED tests/unit/test_events_phase9_1.py::test_action_detail_event_has_step_duration_ms_field
FAILED tests/unit/test_events_phase9_1.py::test_action_detail_event_duration_roundtrip
FAILED tests/unit/test_events_phase9_1.py::test_log_step_puts_duration_on_action_detail_event
3 failed in 0.84s
```

All failures cite `AttributeError: 'ActionDetailEvent' object has no attribute 'step_duration_ms'` — confirming the gap exists and RED gate is established.

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None. This is a test-only file with no stubs.

## Threat Flags

None. Test file only — no new network endpoints, auth paths, or security-relevant surface.

## Self-Check: PASSED

- [x] `tests/unit/test_events_phase9_1.py` exists
- [x] 3 test functions with exact names from must_haves
- [x] Both `from agent.events import ActionDetailEvent` and `from agent.runner import run_agent` imports present
- [x] `asyncio.sleep(0.010)` present
- [x] `patch("agent.runner.Agent", FakeAgentClass)` present
- [x] pytest returns 3 failed (RED) — non-zero exit
- [x] Failures cite `step_duration_ms`
- [x] `git diff --name-only agent/ | wc -l` returns 0 (no production files modified)
- [x] Commit 8a4b1ea verified: `git log --oneline | grep 8a4b1ea`

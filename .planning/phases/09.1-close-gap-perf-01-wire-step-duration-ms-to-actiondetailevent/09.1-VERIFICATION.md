---
phase: 09.1-close-gap-perf-01-wire-step-duration-ms-to-actiondetailevent
verified: 2026-05-18T23:18:57Z
status: human_needed
score: 6/7 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Run `uv run python -m agent`, submit any task (e.g., 'Open https://example.com and report the heading'), watch narration column as agent steps execute."
    expected: "Each narration row shows a small dim timestamp formatted as 'X.Xs' (e.g., '2.4s', '3.2s') on the right edge after the action description. All step rows must have a duration — none missing. Text must use font-size 11px placeholder color consistent with other .timestamp elements."
    why_human: "Visual rendering of the .timestamp span in a live browser run cannot be verified programmatically — requires human observation that the span renders with correct formatting, correct styling, and appears on every narration row."
---

# Phase 09.1: Wire step_duration_ms End-to-End (PERF-01) Verification Report

**Phase Goal:** Wire step_duration_ms end-to-end so users see per-step elapsed time next to each narration entry (PERF-01).
**Verified:** 2026-05-18T23:18:57Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | ActionDetailEvent has a step_duration_ms field of type Optional[int] defaulting to None, appended as the last field | VERIFIED | `agent/events.py` line 79: `step_duration_ms: Optional[int] = None` — confirmed last field on the dataclass |
| 2 | _log_step in agent/runner.py passes step_duration_ms=duration_ms when constructing ActionDetailEvent | VERIFIED | `agent/runner.py` line 578: `step_duration_ms=duration_ms,` inside `_put_nowait(queue, ActionDetailEvent(...))` — `duration_ms` computed at line 541 in scope |
| 3 | handleActionDetail in index.html appends a .timestamp span showing 'X.Xs' when d.step_duration_ms is non-null | VERIFIED | `agent/templates/index.html` lines 452-457: guard `d.step_duration_ms != null && !row.querySelector('.timestamp')`, `toFixed(1) + 's'` formatting present, uses existing `.timestamp` CSS class |
| 4 | All 3 RED tests in tests/unit/test_events_phase9_1.py now pass (GREEN) | VERIFIED | `uv run pytest tests/unit/test_events_phase9_1.py -v` → 3 passed in 0.66s |
| 5 | test_events_phase5.py outdated docstring no longer claims step_duration_ms is unsurfaced | VERIFIED | Line 112 of test_events_phase5.py: "step_duration_ms is computed in _log_step and is surfaced on ActionDetailEvent (PERF-01 wired in Phase 9.1)" — no "no longer surfaced" text remains |
| 6 | test_events_phase6.py shape and null-field tests assert step_duration_ms behavior | VERIFIED | 5 matches of `step_duration_ms` in test_events_phase6.py: default None assert (line 106), `step_duration_ms=250` kwarg (line 116), `== 250` assert (line 124), asdict key present (line 148), asdict value None (line 149) |
| 7 | User visually confirms per-step elapsed time (e.g., '3.2s') renders next to each narration entry during a live run | HUMAN NEEDED | JS wiring exists and is correct; visual rendering requires live browser run with human observer |

**Score:** 6/7 truths verified (7th requires human confirmation per context note)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `agent/events.py` | ActionDetailEvent with step_duration_ms: Optional[int] = None as the last field | VERIFIED | Line 79, confirmed last field position |
| `agent/runner.py` | _log_step emits ActionDetailEvent with step_duration_ms=duration_ms | VERIFIED | Line 578, `duration_ms` in-scope at line 541 |
| `agent/templates/index.html` | handleActionDetail appends .timestamp span when step_duration_ms is non-null | VERIFIED | Lines 452-457, idempotent guard, toFixed(1) format |
| `tests/unit/test_events_phase9_1.py` | 3-test RED gate suite | VERIFIED | All 3 tests GREEN; file exists with exact function names |
| `tests/unit/test_events_phase6.py` | Updated shape and null-field tests asserting step_duration_ms | VERIFIED | 5 occurrences of step_duration_ms across both test functions |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `agent/runner.py::_log_step` | `agent.events.ActionDetailEvent` | constructor kwarg step_duration_ms=duration_ms | WIRED | Line 578 in `_put_nowait(queue, ActionDetailEvent(..., step_duration_ms=duration_ms))` |
| `agent/templates/index.html::handleActionDetail` | ActionDetailEvent.step_duration_ms (SSE payload) | d.step_duration_ms guarded span append | WIRED | Lines 452-457: loose null check + idempotency guard + toFixed(1) + 's' |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `agent/runner.py::_log_step` | `duration_ms` | `int((time.monotonic() - step_start) * 1000)` at line 541 | Yes — wall-clock measurement from step_start to on_step_end invocation | FLOWING |
| `agent/templates/index.html::handleActionDetail` | `d.step_duration_ms` | SSE JSON payload from ActionDetailEvent serialized via dataclasses.asdict | Yes — flows from runner through queue → SSE → JS handler | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 3 phase9_1 unit tests pass | `uv run pytest tests/unit/test_events_phase9_1.py -v` | 3 passed in 0.66s | PASS |
| Full unit suite (excluding pre-existing phase8 failure) | `uv run pytest tests/unit/ -x --ignore=tests/unit/test_events_phase8.py` | 191 passed in 16.27s | PASS |
| step_duration_ms field is last on dataclass | `grep -n "step_duration_ms: Optional\[int\] = None" agent/events.py` | line 79 (last line of dataclass) | PASS |
| runner wires the kwarg | `grep -nE "step_duration_ms=duration_ms" agent/runner.py` | 1 match at line 578 | PASS |
| JS guard present | `grep -nE "d\.step_duration_ms != null" agent/templates/index.html` | 1 match at line 452 | PASS |
| JS toFixed formatting | `grep -nE "toFixed\(1\) \+ 's'" agent/templates/index.html` | 1 match at line 455 | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| PERF-01 | 09.1-01, 09.1-02 | User sees per-step elapsed time next to each narration entry (e.g., "3.2s") | SATISFIED (unit-level) / HUMAN NEEDED (visual) | Dataclass field, runner kwarg, and JS span all wired and tested. Visual confirmation deferred to human per context note. |

**Note on REQUIREMENTS.md traceability table:** The table at the bottom of REQUIREMENTS.md still maps PERF-01 to "Phase 5 / Pending" and the checkbox at line 10 remains unchecked. This is a documentation artifact only — the implementation exists in the codebase. The traceability row and checkbox should be updated to reflect Phase 9.1 / Complete.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | — | — | No debt markers, stubs, or placeholder patterns found in modified files |

**Commit hash discrepancy (informational):** The 09.1-02-SUMMARY.md documents commit hashes `0fdf9ae` and `5e8a049` but the actual commits in git history are `021da30` and `3228962`. The code is correct and present; this is a SUMMARY documentation error only, not a code defect.

### Human Verification Required

#### 1. Live browser rendering of per-step elapsed time

**Test:** Run `uv run python -m agent` from the project root. Submit any simple task in the UI (e.g., "Open https://example.com and report the heading text"). Watch the narration column as agent steps execute.

**Expected:**
- Each narration row shows, from left to right: action badge (colored), step number, action description, and on the right edge a small dim timestamp like "2.4s" or "3.2s" or "0.0s"
- Format must be `X.Xs` with exactly one decimal place — not "NaNs", not "undefineds", not "3.234567s"
- The timestamp span must be styled as small dim text (font-size 11px, placeholder color — consistent with other `.timestamp` elements)
- Every step row must have a duration — none missing

**Why human:** Visual rendering of the .timestamp DOM span cannot be verified programmatically. The JS wiring is confirmed correct (guard, createElement, toFixed(1)), but whether it actually renders in the browser with correct styling and appears on all rows requires a live run with a human observer.

**Resume signal:** Type "approved" if all narration rows show properly formatted "X.Xs" duration in dim text. Otherwise describe the specific issue.

### Gaps Summary

No code gaps. All unit-level wiring is complete and all automated tests pass. The only open item is the human visual confirmation (Task 3 from Plan 02), which was explicitly deferred by the user to the next milestone's visual refactor and is classified here as `human_needed` rather than a gap.

---

_Verified: 2026-05-18T23:18:57Z_
_Verifier: Claude (gsd-verifier)_
